<Window x:Class="TaskManager.TaskInputWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="新規タスク" Height="300" Width="400"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- タイトル入力 -->
        <Label Grid.Row="0" Content="タイトル"/>
        <TextBox x:Name="TitleTextBox" 
                 Grid.Row="1" 
                 Margin="0,0,0,10"/>

        <!-- メモ入力 -->
        <Label Grid.Row="2" Content="メモ"/>
        <TextBox x:Name="MemoTextBox" 
                 Grid.Row="3" 
                 Height="80"
                 TextWrapping="Wrap"
                 AcceptsReturn="True"
                 VerticalScrollBarVisibility="Auto"
                 Margin="0,0,0,10"/>

        <!-- 予定時間入力 -->
        <Grid Grid.Row="4">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            <Label Content="予定時間"/>
            <StackPanel Grid.Row="1" 
                      Orientation="Horizontal" 
                      VerticalAlignment="Top">
                <TextBox x:Name="HoursTextBox" 
                         Width="50" 
                         Margin="0,0,5,0"/>
                <Label Content="時間"/>
                <TextBox x:Name="MinutesTextBox" 
                         Width="50" 
                         Margin="10,0,5,0"/>
                <Label Content="分"/>
            </StackPanel>
        </Grid>

        <!-- ボタン -->
        <StackPanel Grid.Row="5" 
                    Orientation="Horizontal" 
                    HorizontalAlignment="Right" 
                    Margin="0,20,0,0">
            <Button Content="キャンセル" 
                    Width="80" 
                    Margin="0,0,10,0"
                    Click="CancelButton_Click"/>
            <Button Content="追加" 
                    Width="80"
                    Click="AddButton_Click"/>
        </StackPanel>
    </Grid>
</Window>