using System;

namespace TaskManager
{
    public enum TaskStatus
    {
        InProgress,
        Pending,
        Completed
    }

    public enum TaskPriority
    {
        Low,
        Medium,
        High
    }
}